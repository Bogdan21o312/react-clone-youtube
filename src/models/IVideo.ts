export interface IVideo {
    id: number,
    logo: string,
    title: string,
    text: string,
    channel: string,
    isVerified: boolean,
    category: string,
    duration: string,
    views: number,
    uploadTime: string
}